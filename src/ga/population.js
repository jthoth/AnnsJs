class Population{

  constructor(objective, mutationRate, numPoblation){
      this.objective = objective
      this.mutationRate = mutationRate
      this.numPoblation = numPoblation
  }

}

module.exports = {
  Population,
}
